// Get background size plugin | (c) 2013 Richard Tan// Used for getting the original image size of a background image( function($) {	$.fn.backgroundSize = function(property) {		//-------GET BACKGROUND-IMAGE URL------------------------		bgURL = $(this).css('background-image');			//-------TRIM OFF EXTRAS------------------------		//IF LOCAL FILE		if( bgURL.search('file:///') > -1 ) {			//trim off front portion			a = bgURL.indexOf('file:///');			bgURL = bgURL.substr(a);		} 		//IF IT'S FROM THE WEB		else if ( bgURL.search('http') > -1 ) {			//trim off front portion			a = bgURL.indexOf('http');			bgURL = bgURL.substr(a);		}				//trim off final bracket		bgURL = bgURL.slice(0, -1);		//if double quote remains on end, trim it off		if( bgURL.slice(-1) == '"'){			bgURL = bgURL.slice(0, -1);		}				//-------INSERT INTO <IMG> ELEMENT------------------------		$('body').append('<img class="temporaryimg" src="' + bgURL + '" />');		$('.temporaryimg')			.css('width','auto')			.css('height','auto')			.css('display','none');				//-------GET WIDTH & HEIGHT------------------------		width = $('.temporaryimg').width();		height = $('.temporaryimg').height();				//-------DELETE <IMG> ELEMENT------------------------		$('.temporaryimg').remove();				//-------RETURN THE VALUES------------------------		if(property == 'width') {			return(width);		}		if(property == 'height') {			return(height);		}					}}) (jQuery);